echo "# gitHubPractise" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/anupama95singh/gitHubPractise.git
git push -u origin master

git remote add origin https://github.com/anupama95singh/gitHubPractise.git
git push -u origin master

We recommend every repository include a README, LICENSE, and .gitignore.

#############################################################################
git --version

git config --global user.name "anupama95singh"
git config --global user.email "anupama.singh@triconinfotech.com"

git config --list

git help config
git config --help

git init
//initialise an empty git repo

rm -rf .git
//remove a .git repo

git status

touch .gitignore

git commit -m "initial commit blah blah"

git add -A
//add every thing to staging area

git log

git clone <url> --b <branch-name>

git branch -a

git remote -v

git diff

git pull origin <branch_name>

git branch anupama
//ceates a new branch anupama

git branch
//all the branches

git checkout anupama
//move from current to anupama

git push -u origin anupama
git branch -a
// -u is used for associating local anupama branch to remote anupama branch
git pull
git push

############################################################################
merge a branch anupama to master

git checkout master
git pull origin master
git branch --merged
git merge anupama 
git push origin master

#############################################################################
delete a branch anupama from local
git branch -d anupama

delete a branch anupama from remote
git push origin --delete anupama

for more help visit: https://www.youtube.com/watch?v=HVsySz-h9r4

